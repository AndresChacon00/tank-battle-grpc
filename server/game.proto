syntax = "proto3";
package game;

option go_package = "/game";

service GameService {
    rpc UpdateState (PlayerState) returns (GameState);
}

message PlayerState {
    string player_id = 1;
    float x = 2;
    float y = 3;
}

message GameState {
    repeated PlayerState players = 1;
}

